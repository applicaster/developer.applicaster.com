machine:
  node:
    version: 5.0.0
general:
  branches:
    ignore:
      - master
dependencies:
  pre:
    - npm install -g npm@3.3.9
  override:
    - npm install
deployment:
  production:
    branch: developer2
    heroku:
      appname: applicaster-developer
