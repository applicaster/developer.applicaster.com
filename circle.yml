machine:
  node:
    version: 5.3.0
general:
  branches:
    ignore:
      - master
dependencies:
  pre:
    - npm install -g npm@3.3.12
deployment:
  production:
    branch: developer2
    heroku:
      appname: applicaster-developer
